{
  "name": "@flip-master/connectors",
  "version": "0.1.0",
  "private": true,
  "description": "{{BRAND_NAME}} connector layer — Property Data Hub + swappable ATTOM/HouseCanary/MLS adapters feeding the valuation engine.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "pipeline": "node examples/pipeline.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
