{
  "name": "@flip-master/api",
  "version": "0.1.0",
  "private": true,
  "description": "ValueProof licensing API (§9) — framework-free HTTP surface: valuations, reports, watches, capture sessions, scope-of-work, webhooks. API-key auth + usage metering.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "start": "node examples/serve.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
