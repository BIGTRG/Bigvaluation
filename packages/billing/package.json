{
  "name": "@bigvaluation/billing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ValueProof Billing — Stripe connector (§5): pay-per-report checkout, Pro membership subscriptions, metered partner usage, webhook-driven plan sync.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {}
}
