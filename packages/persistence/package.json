{
  "name": "@flip-master/persistence",
  "version": "0.1.0",
  "private": true,
  "description": "ValueProof persistence (§8) — Postgres schema + stores behind an injectable SqlClient. The real pg adapter is opt-in.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./pg": "./src/pgClient.ts"
  },
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "optionalDependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/pg": "^8.11.0"
  }
}
