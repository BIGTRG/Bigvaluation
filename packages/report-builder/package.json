{
  "name": "@flip-master/report-builder",
  "version": "0.1.0",
  "private": true,
  "description": "ValueProof report builder — renders a Valuation into a branded, self-contained HTML report (web + PDF source). Not an appraisal.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "generate": "node examples/generate.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
